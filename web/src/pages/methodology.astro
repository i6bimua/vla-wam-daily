---
import BaseLayout from "../layouts/BaseLayout.astro";
import { loadLatestDataFile } from "../lib/data";
import { formatBeijingTimestamp } from "../lib/time";

const latest = await loadLatestDataFile();
const generatedAtLabel = formatBeijingTimestamp(latest.generated_at);
const hasPublishedPapers = latest.papers.length > 0;
const models = [
  ...new Set(latest.papers.map((paper) => paper.provenance.model)),
].sort();
const promptVersions = [
  ...new Set(latest.papers.map((paper) => paper.provenance.prompt_version)),
].sort();
const repository = import.meta.env.GITHUB_REPOSITORY;
const issuesUrl = repository ? `https://github.com/${repository}/issues` : null;
---

<BaseLayout
  title="方法与透明度 · VLA/WAM Daily"
  description="了解 VLA/WAM Daily 的数据来源、筛选、AI 分析、图片提取与发布标准。"
>
  <header class="route-hero">
    <p class="eyebrow">METHODOLOGY</p>
    <h1>方法与透明度</h1>
    <p>
      当前数据生成于 <time datetime={latest.generated_at}
        >{generatedAtLabel}</time
      >。
    </p>
    {
      hasPublishedPapers ? (
        <p>
          当前已发布论文记录的分析模型为
          <strong>{models.join("、")}</strong>，Prompt 版本为{" "}
          {promptVersions.join("、")}。
        </p>
      ) : (
        <p>暂无已发布论文，无法从当前数据确认模型或 Prompt。</p>
      )
    }
    <p>
      仓库 quality profile 的默认模型为
      deepseek-v4-pro；运行时配置可以覆盖该默认值。
    </p>
  </header>

  <article class="prose">
    <section>
      <h2>数据来源与更新</h2>
      <p>
        论文元数据来自 arXiv。默认配置关注 cs.RO、cs.CV、cs.AI 和
        cs.LG；运行时可通过 --config-path 使用其他配置，因此当前 DataFile
        不证明本次实际分类范围。站点默认按北京时间每日更新，但具体执行时间和频率可在
        GitHub Actions 工作流中配置。
      </p>
    </section>

    <section>
      <h2>两级筛选</h2>
      <p>
        第一层用完整概念词和“模型概念 +
        机器人动作”组合做确定性预筛，避免把整个大分类送入模型。第二层由当前分析模型根据论文标题与摘要给出相关性评分、主题和结构化研究笔记。
      </p>
    </section>

    <section>
      <h2>评分与发布阈值</h2>
      <ul>
        <li>9–10：VLA、WAM 或机器人动作世界模型是论文核心。</li>
        <li>7–8：强相关的方法、数据集、评测或通用机器人策略。</li>
        <li>6：对该方向有直接价值的相邻研究。</li>
        <li>1–5：相关性较低；是否发布取决于本次运行配置。</li>
      </ul>
      <p>
        仓库默认/示例发布阈值为 6，运行时可通过 CLI --threshold 覆盖。当前
        DataFile schema
        不持久化实际阈值，因此当前构建数据不携带实际运行阈值，页面不能据此确认本次阈值。
      </p>
      <p>
        AI
        分析范围严格限于标题与摘要；摘要没有提供的实验结果、局限或机构信息会明确标注为未说明。
      </p>
    </section>

    <section>
      <h2>资源与 Fig. 1 / Fig. 2</h2>
      <p>
        项目主页和代码链接只在来源数据明确提供时展示，不会猜测资源地址。Fig. 1 /
        Fig. 2 从 arXiv HTML
        提取并以远程图片展示，提取结果写入缓存以减少重复请求；HTML、图片或网络不可用时，页面会保留明确的降级状态，并提供
        arXiv HTML 或 PDF 入口。
      </p>
    </section>

    <section>
      <h2>局限与反馈</h2>
      <p>
        关键词与摘要级判断都可能产生误报或漏报，新术语也可能暂时未被覆盖。{
          issuesUrl ? (
            <a href={issuesUrl} target="_blank" rel="noopener noreferrer">
              请通过 GitHub Issues 提交反馈
            </a>
          ) : (
            "请通过项目仓库的 GitHub Issues 提交反馈"
          )
        }。
      </p>
    </section>
  </article>
</BaseLayout>
