# VLA/WAM Daily

Daily VLA and World Action Model research portal.

## Data directory

The repository includes the required `data/` directory. When using a custom
`data_dir`, create its parent directory first; the application may create only
the final `data_dir` component and fails closed when its parent chain is missing.
